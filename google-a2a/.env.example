# 1Claw API key (for the worker agent to access the vault)
ONECLAW_API_KEY=ocv_your_key_here

# Vault UUID that contains the secrets the worker needs
ONECLAW_VAULT_ID=your-vault-uuid

# Optional: agent UUID (enables agent-level access policies)
ONECLAW_AGENT_ID=

# OpenAI API key (for the coordinator's LLM reasoning)
OPENAI_API_KEY=sk-...

# Google/Gemini API key (for the ADK demo — Gemini-powered agent)
# ADK reads GEMINI_API_KEY or GOOGLE_GENAI_API_KEY
GOOGLE_API_KEY=your-google-api-key
GEMINI_API_KEY=your-google-api-key

# Worker agent port (default: 4100)
WORKER_PORT=4100

# 1Claw API base URL
ONECLAW_BASE_URL=https://api.1claw.xyz

# --- ECDH demo (optional): store each agent's keys in 1Claw (two accounts) ---
# Create two vaults (or use two accounts). Then run: npm run ecdh:bootstrap
# to generate and store keys. After that, run npm run ecdh with these set.
# ONECLAW_ALICE_VAULT_ID=
# ONECLAW_ALICE_API_KEY=
# ONECLAW_BOB_VAULT_ID=
# ONECLAW_BOB_API_KEY=
# Optional agent IDs for agent-level policies:
# ONECLAW_ALICE_AGENT_ID=
# ONECLAW_BOB_AGENT_ID=

# Then run: npm run ecdh:test (bootstrap → demo → cleanup)
# Or run cleanup only: npm run ecdh:cleanup
