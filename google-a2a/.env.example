# 1Claw API key (for the worker agent to access the vault)
ONECLAW_API_KEY=ocv_your_key_here

# Vault UUID that contains the secrets the worker needs
ONECLAW_VAULT_ID=your-vault-uuid

# Optional: agent UUID (enables agent-level access policies)
ONECLAW_AGENT_ID=

# OpenAI API key (for the coordinator's LLM reasoning)
OPENAI_API_KEY=sk-...

# Worker agent port (default: 4100)
WORKER_PORT=4100

# 1Claw API base URL
ONECLAW_BASE_URL=https://api.1claw.xyz
