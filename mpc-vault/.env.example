# Human API key from https://1claw.xyz/settings/api-keys (1ck_ prefix).
# MPC vault creation requires the right billing tier on the org (see README).
ONECLAW_BASE_URL=https://api.1claw.xyz
ONECLAW_API_KEY=1ck_your_key_here

# Set to 1 to keep demo vaults after a run (default: delete vaults on success).
# MPC_SKIP_CLEANUP=0
