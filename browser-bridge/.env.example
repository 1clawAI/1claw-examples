# Optional — only needed if Chrome/Chromium isn't at the default path this
# demo checks (macOS: /Applications/Google Chrome.app/..., Linux: /usr/bin/google-chrome).
# ONECLAW_BRIDGE_CHROME=/path/to/chrome
