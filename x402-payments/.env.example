# 1Claw API (user or agent)
ONECLAW_BASE_URL=https://api.1claw.xyz
ONECLAW_API_KEY=
ONECLAW_VAULT_ID=
# Optional: for agent-scoped auth and Intents API demos
ONECLAW_AGENT_ID=

# x402 payment signer (EOA on Base). Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Ensure this wallet has USDC on Base for real payments when over quota.
X402_PRIVATE_KEY=
