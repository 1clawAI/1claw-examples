# OpenAI API key (for the LLM that drives the agent)
OPENAI_API_KEY=sk-...

# 1Claw API key (user or agent key)
ONECLAW_API_KEY=ocv_your_key_here

# Vault UUID that the agent will read secrets from
ONECLAW_VAULT_ID=your-vault-uuid

# 1Claw Agent ID (optional — omit to authenticate as a human user)
ONECLAW_AGENT_ID=

# 1Claw Agent JWT (required for the MCP client example)
ONECLAW_AGENT_TOKEN=

# 1Claw API base URL (default: https://api.1claw.xyz)
ONECLAW_BASE_URL=https://api.1claw.xyz
