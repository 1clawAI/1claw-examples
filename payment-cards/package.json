{
  "name": "@1claw/example-payment-cards",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "description": "Payment Card Vault — order a prepaid card via x402, poll to ready, reveal with human re-auth.",
  "type": "module",
  "scripts": {
    "start": "npx tsx --env-file=.env src/index.ts",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "@1claw/sdk": "^0.41.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
