# ── Wallet Configuration ─────────────────────────────────────────────
#
# Option A: provide the session key directly.
# BUYER_PRIVATE_KEY=0x...
#
# Option B: leave BUYER_PRIVATE_KEY unset and store the key in your
# 1Claw vault. The bootstrap fetch uses your free tier / credits.
# BUYER_KEY_PATH=keys/x402-session-key
#
# If the buyer key is a signer on a smart account (ERC-4337 / Safe),
# set the smart account address. When set, SmartAccountWallet (ERC-1271)
# is used instead of a plain EOA wallet.
# SMART_ACCOUNT_ADDRESS=0x...

# ── 1Claw Credentials (required) ────────────────────────────────────
ONECLAW_API_KEY=ocv_your_key_here
ONECLAW_VAULT_ID=your-vault-uuid

# Agent UUID — required for the MCP example, optional for HTTP/hybrid.
# ONECLAW_AGENT_ID=your-agent-uuid

# API base URL (default: https://api.1claw.xyz)
# ONECLAW_BASE_URL=https://api.1claw.xyz

# ── Coinbase CDP (required for x402 server/client demo) ─────────────
# Get these from https://portal.cdp.coinbase.com/
# CDP_API_KEY_ID=your-cdp-key-id
# CDP_API_KEY_SECRET=your-cdp-key-secret

# ── x402 Server Demo ────────────────────────────────────────────────
# Wallet address that receives x402 payments (any address you control).
# X402_PAY_TO_ADDRESS=0x...
# X402_SERVER_PORT=4021
# X402_SERVER_URL=http://localhost:4021
